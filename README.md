# Gene-Signature-Predictability-Prediction

##Getting started
Download the github repository:
```
git clone
```

Download required packages in R (using Rstudio):
```
install.packages("survival")
install.packages("randomForest")
```
